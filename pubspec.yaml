name: cag
description: CLI wrapper for AI agents (gemini, claude, etc.)
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  args: ^2.6.0
  json_schema: ^5.2.2
  mcp_dart: ^1.1.2
  path: ^1.9.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
