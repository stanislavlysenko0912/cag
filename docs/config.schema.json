{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "cag config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "agents": {
      "type": "object",
      "description": "Per-agent overrides (claude, gemini, codex, cursor, antigravity). Use this to customize executable paths, args, env, timeouts, or to enable shell mode.",
      "propertyNames": {
        "enum": [
          "claude",
          "gemini",
          "codex",
          "cursor",
          "antigravity"
        ]
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "executable": {
            "type": "string",
            "description": "Executable name or full path. On Windows, you can point to a .exe/.cmd/.bat shim if PATH resolution fails."
          },
          "enabled": {
            "type": "boolean",
            "description": "Enable or disable this agent."
          },
          "default_model": {
            "type": "string",
            "description": "Default model name used when none is provided on the command line."
          },
          "models": {
            "type": "array",
            "description": "Additional or replacement model definitions exposed by CAG for this agent.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "name"
              ],
              "properties": {
                "name": {
                  "type": "string"
                },
                "description": {
                  "type": "string"
                },
                "model": {
                  "type": "string",
                  "description": "Model identifier passed to the wrapped agent when it differs from name."
                },
                "is_default": {
                  "type": "boolean"
                },
                "aliases": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              }
            }
          },
          "additional_args": {
            "type": "array",
            "description": "Arguments always appended to the command.",
            "items": {
              "type": "string"
            }
          },
          "shell_executable": {
            "type": "string",
            "description": "Optional shell executable (e.g., /bin/zsh, cmd, wsl.exe). When set with shell_command_prefix, the command runs through this shell."
          },
          "shell_args": {
            "type": "array",
            "description": "Arguments passed to the shell executable (e.g., -i -c, /c, -e bash -lc).",
            "items": {
              "type": "string"
            }
          },
          "shell_command_prefix": {
            "type": "string",
            "description": "Prefix command used in shell mode. The prompt and args are appended after this string."
          },
          "env": {
            "type": "object",
            "description": "Environment variables for the agent process.",
            "additionalProperties": {
              "type": "string"
            }
          },
          "hard_timeout_seconds": {
            "type": "integer",
            "description": "Hard process timeout in seconds.",
            "minimum": 1
          },
          "idle_timeout_seconds": {
            "type": "integer",
            "description": "Idle timeout in seconds based on no stdout/stderr activity.",
            "minimum": 1
          }
        }
      }
    }
  }
}
